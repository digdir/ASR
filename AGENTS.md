# Repository Instructions

## Shared GitHub Workflow

Follow the shared workflow in `README.md` before editing or pushing. In short:
always verify that local `main` is based on the latest `origin/main`, update
before editing when needed, preserve any local uncommitted changes, and check
GitHub again before pushing.
